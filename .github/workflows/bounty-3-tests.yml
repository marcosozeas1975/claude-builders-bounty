name: Bounty 3 guard tests

on:
  push:
    branches:
      - bounty-3-destructive-hook
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run guard tests
        run: python3 hooks/destructive-command-guard/test_block_destructive.py
